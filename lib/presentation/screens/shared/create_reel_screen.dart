import 'package:flutter/material.dart';

class CreateReelScreen extends StatelessWidget {
  static const String name = 'create-reel-screen';
  const CreateReelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}